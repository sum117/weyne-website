# dependencies
node_modules

# build output
.output
.nitro
.tanstack
dist
.vite

# generated route tree
# (committed is fine too, but it is regenerated on build)

# test / tooling output
artifacts
coverage
playwright-report
test-results
blob-report
tests/e2e/__screenshots__
spikes/**/dist

# env
.env
.env.local
.env.*.local
# production secrets (live only on the VPS)
.env.weyne
deploy/.env.weyne
deploy/.env.storage
deploy/.env.staging

# logs
*.log
npm-debug.log*
bun-debug.log*

# editor / OS
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
Thumbs.db

# local agent/tooling config and scratch (personal)
.claude/
.hermes/
.hermes-tmp.*
**/.hermes-tmp.*
